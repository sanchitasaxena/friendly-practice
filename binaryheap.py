# heap operations

# insert - (bubble up , comparing to parent and if not in correct order swap)
# the number of operations depends on the number of levels the new element
# must rise to satisfy the heap properly giving worst case complexity of O(log n)
# average case is O(1)

